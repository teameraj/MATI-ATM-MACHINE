struct Transaction 
{
     double amount;
     int paymentMethod;
     int transactionType; // 0 for withdrawal, 1 for deposit
     struct Transaction* next;
 };
struct Account
{
    int id;
    int PIN;
    float balance;
    struct Transaction* transactionList; //including transaction structure into account structure
    struct Account* next;
};
